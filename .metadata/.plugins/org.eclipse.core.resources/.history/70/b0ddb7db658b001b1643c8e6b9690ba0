package entidades;

public class Triangulo {
	
	public double a;
	public double b;
	public double c;
	
	public Double area () {
		double p = (a + b + c ) /2;
		double retorno = Math.sqrt(p * (p - a) * (p - b) * (p - c));
	}
}
